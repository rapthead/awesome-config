-- Standard awesome library
require("awful.autofocus")

-- Decomposition
local mymainmenu        = require("conf.menu")
local keybindings       = require("conf.keybindings")
local rules             = require("conf.rules")
local notifications     = require("conf.notifications")
local signals           = require("conf.signals")
local layout            = require("conf.layout")
